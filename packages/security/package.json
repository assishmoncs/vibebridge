{
  "name": "@vibebridge/security",
  "version": "0.1.0",
  "description": "Security sandbox, filesystem tools, and terminal execution for VibeBridge",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@vibebridge/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  }
}
